const initialState = {
    addedUser: {
        email: null,
        isAdmin: false,
    },
    success: false,
};

const RegistrationReducer = (state=initialState, action)=>{
    return state
};

export default RegistrationReducer